/**
 * Возвращает первые limit слов из words, которые начинаются с query
 * (без учёта регистра). Слова возвращаются в исходном регистре.
 *
 * @param {string[]} words
 * @param {string} query
 * @param {number} limit
 * @returns {string[]}
 */
function topWords(words, query, limit) {
  // TODO: реализовать
  throw new Error('not implemented');
}

module.exports = { topWords };
